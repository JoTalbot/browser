# Итерация: аудит + устранение P0/P1 (продолжение шага step_90_full_audit)

- 🖥️ Агент: Arena.ai Agent Mode (arm-server-01, OCI). Решения владельца: P0-1 приватность, P0-2 закрыть снаружи, P1 — все фиксы, P2 — на усмотрение агента, durability — план.
- 📄 Полный отчёт: /root/agents/-Octopus/reports/AUDIT_2026-09-17_fixes_and_durability_plan.md
- 💾 Бэкапы всех изменённых файлов: /root/backups/audit-2026-09-17/

## Сделано
1. P0-1 HF-утечка: private=True отклонён (403, private storage limit) → удалены 214 файлов (чанки ×212, bootstrap, mesh_nodes) + dr_manifest.json;
   внешний доступ к чанкам/манифесту/bootstrap = 404. Создан приватный JoTalbot/octopus-eternal-private.
   eternal-snapshot: HF-чанки по умолчанию не публикуются (OCTOPUS_ETERNAL_HF_CHUNKS!=1), дефолт HF_REPO_ID — приватный,
   в tar добавлены exclude etc/octopus, *.pem, *_token (dry-run: 0 файлов из /etc/octopus, было 67). secrets.env: +HF_REPO_ID.
2. P0-2 noVNC: ufw ALLOW 6080 удалены + deny; DROP в DOCKER-USER + новый юнит octopus-firewall-hardening.service (enabled, идемпотентный);
   контейнер octopus-browser-chromium пересоздан на 127.0.0.1:6080/9222 (старый сохранён как .bak.20260917T053349Z, остановлен).
   Проверки: loopback noVNC 200, CDP Chrome/152, cookie-keeper «vault updated: 18 cookies», снаружи — таймаут/0 байт.
3. P0-3 хардкод HF-токена в /opt/octopus/octopus-multisync.py:24 → убран, чтение из env/secrets.env (_load_secrets_env), сервис перезапущен (active).
4. P1-фиксы (все с бэкапами и проверкой):
   - octopus-health-json.sh — атомарная запись + сводка в stderr (health.json стал валидным JSON);
   - octopus-critical-guard.sh и octopus-self-heal.sh — пропуск отсутствующих юнитов (шум octopus-devpanel устранён, новые события после 05:44 отсутствуют);
   - cookie-keeper — timeout CDP 15s, goto 25s, обёртка исключений → exit 0 (ранее 666 failed/24ч);
   - octopus-slo-checker.py — return 0 вместо 2/1 (ранее 569 failed/24ч); прежнее поведение: SLO_CHECKER_FAIL_ON_RED=1.
5. P2: drop-in limits.conf для eternal-snapshot (MemoryHigh=6G, MemoryMax=10G, TasksMax=256; пик был 9.6G);
   journald SystemMaxUse=500M + vacuum (1.0G → 478.5M); docker builder prune (освобождено 57kB, остальное занято образами);
   ARENA_HEALTH_RUNBOOK_RU.md — секция актуализации (битые пути к инструментам + фактические точки проверки).

## Итоговое состояние (проверено)
- SLO 15/15 green, rc=0; failed-юнитов 0; health.json валиден (svcs 34/161, disk avail 92.9G);
- сервисы active: multisync, aios, browser, browser-aios-adapter, cas-api, firewall-hardening;
- контейнер браузера Up, порты только loopback.

## Открыто (за владельцем)
- 🔴 Ротация всех секретов из /etc/octopus/secrets.env + HF-токена (в истории git и в копиях) + SSH-ключа, отправленного в чат.
- 🟠 Off-host копии памяти = 0: реализовать план A (OCI Object Storage: packstore + дамп app_db + манифест sha256) и починить memory_copies_audit (сейчас 0/0=1.0).
- 🟠 Убрать хардкод-токен из /opt/octopus-aios-integration/octopus-multisync.py и /opt/orchestrator/data/{chats,light}/*.json (4 файла).
- 🟡 Решение по git-расхождению /opt/octopus (main 92↔64, HEAD 101↔64, 5 веток, worktrees batch18/19/20-oci, wt*).
- 🟡 Права /root/agents (135 каталогов drwxrwxrwx) — менять только после ревизии, какие сервисы пишут туда от ubuntu.
- 🟡 Пустой публичный датасет JoTalbot/octopus-eternal: приватность недоступна (лимит) → оставить пустым или удалить (ломает DR-bootstrap ссылку).

## Следующий шаг
- Ждать решения владельца по ротации и по варианту A (durability). Готов реализовать A1–A4 в этой сессии.
