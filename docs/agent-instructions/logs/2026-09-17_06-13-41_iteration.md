# Итерация: durability памяти + реестр секретов + зачистка утечек (step_90_full_audit, часть 2)

- 🖥️ Агент: Arena.ai Agent Mode (arm-server-01, OCI). Решения владельца: «Делай всё», «секреты Октопус должен помнить все и всегда — какие они, зачем и откуда», «можешь закрыть репо».
- 📄 Отчёт: /root/agents/-Octopus/reports/AUDIT_2026-09-17_durability_secrets_registry.md

## Сделано
1. 📦 Off-host копия памяти (впервые за всю историю наблюдений):
   - новый скрипт /opt/octopus-packstore-offhost-oci.py (инкрементальная загрузка packstore в OCI Object Storage,
     manifest.json с sha256, proof-of-read выборка) — юнит octopus-packstore-offhost.service переключён на него;
   - результат: 45 файлов / 270.8 MB загружено, verify 5/5 sha256 OK, missing=0, 8.3 c, пик RAM 19.5 MB (лимит 200M);
   - прежний канал (rsync на ноды memory-replica) мёртв: все AWS-ноды в nodes.json en=False, ключа нет → targets=0.
2. 📊 Честный аудит копий: /opt/octopus-memory-copies-audit.py считает packstore + бэкенд OCI Object Storage,
   креды Garage вынесены из хардкода в env, 0/0 больше не даёт coverage=1.0; атомарная запись результата в юните.
   Итог: packstore_files=45, covered=45, coverage=1.0, pack_index_v2_refs=20524, ok=true.
3. 🎯 SLO: добавлены чеки packstore_offhost_copies (P0), packstore_offhost_sync_recent (P1), memory_copies_audit_fresh (P2);
   исправлен ложно-зелёный memory_independent_copy_coverage_1_0 (пустой memory_pool не считается покрытием).
   Итог: 18/18 PASS, status=green.
4. 💾 OCI Vault снимок: исправлен пустой дамп БД (pg_dumpall от root падал на peer-auth; теперь sudo -u postgres + /tmp +
   перенос, chmod 600) → databases.sql 18.3 MB в архиве (было 0 байт); исправлена ротация/квота
   (list_objects без fields='name,size,timeCreated' давал size=None → квота всегда 0, sorted() по None падал бы);
   packstore/ исключён из ротации (живая копия памяти). Факт: бакет 0.30 GB из 18 GB, 64 объекта.
5. 🔐 Реестр секретов (требование владельца): /opt/octopus/secrets-registry/octopus-secrets-registry.py
   → /var/lib/octopus/secrets_registry.{json,md} (chmod 600). 125 записей: 64 секрета + 61 служебная переменная,
   у всех 64 секретов описано назначение; значения НЕ хранятся — только sha256[:12], длина, источник, first_seen,
   история смены отпечатка (ротации). Режим hunt ищет хардкод и связывает находки с реестром по отпечатку.
   Ежедневный скан: octopus-secrets-registry.timer (02:40). Реестр добавлен в снимок OCI Vault.
6. 🧹 Зачистка утечек:
   - публичный HF-датасет JoTalbot/octopus-eternal удалён (delete_repo) — приватным стать не мог (403 private storage limit);
     канал публикации переведён на приватный JoTalbot/octopus-eternal-private (multisync уже пишет туда);
   - /etc/octopus полностью исключён из мастер-архива (убран из списка путей tar + exclude);
   - хардкод HF-токена убран из /opt/octopus/octopus-multisync.py (чтение из secrets.env);
   - редукция секретов в данных оркестратора: 546 файлов просканировано, 17 изменено, 43 вхождения
     (ghp_, AKIA, bot-токен, sk-), JSON-целостность проверена (0 сломанных), бэкапы 64 MB (chmod 600).
7. 🌿 Git: правки зафиксированы в /opt/octopus коммитом 16b5767 в ветке arena/audit-2026-09-17-security-hardening
   и запушены в origin (ветка создана). main не трогали.
8. 🧬 Skill: /root/agents/-Octopus/skills/core/octopus-exposure-audit-2026-09-17 (SKILL.md + code/check_public_exposure.py
   + tests/test_contract.py + references/incident_2026-09-17.md). Контракт-тест: 6/6 ✅. Индекс скиллов пересобран: 0 stubs, 0 duplicates.

## Находки, требующие решения владельца
- 🔴 Dependabot: 36 открытых алертов в JoTalbot/octopus (3 critical: Next.js RCE ×2, next-auth email-normalizer bypass) —
  это Next.js Admin UI (octopus-next-admin). Нужен апгрейд next/next-auth.
- 🟠 Git: локальная история и origin/main — НЕЗАВИСИМЫЕ (разные корневые коммиты 6dfecb2 vs 802ca7e), merge-base отсутствует,
  diff 3966 файлов. Прод-сервисы работают из локального дерева (/opt/octopus, ветка arena/...). Варианты:
  (a) объявить локальную историю канонической и заменить origin/main (force, деструктивно для чужого среза),
  (b) держать две линии и переносить нужное cherry-pick, (c) склеить через --allow-unrelated-histories (3966 файлов конфликтов).
- 🟠 Ротация секретов обязательна (публичная экспозиция архива + хардкод токена + токен в чатах и в истории git).
- 🟡 Публичные ua-* датасеты/модели на HF (7 шт.) отдаются анонимно — по смыслу это открытые данные (УДР/открытые данные/legal-LM),
  секретов в них не найдено; оставлены как есть, зафиксированы в реестре экспозиций.
- 🟡 /opt/octopus-aios-integration — worktree с незакоммиченным патчем multisync (мёртвая копия с прежним хардкодом).

## Следующий шаг
- Ждать решений: ротация секретов, вариант склейки git, апгрейд next/next-auth.
