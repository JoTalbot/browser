name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11', '3.12', '3.13']
    steps:
      - name: Checkout
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e '.[dev]'
          pip install ./integrations/browser-aios-adapter
      - name: Ruff
        run: ruff check .
      - name: Pytest
        run: pytest -q
      - name: Dependency audit
        if: matrix.python-version == '3.13'
        run: |
          python -m pip install pip-audit
          pip-audit
      - name: Build
        run: python -m build

  adapter:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.12'
      - name: Install adapter
        run: |
          python -m pip install --upgrade pip
          python -m pip install ./integrations/browser-aios-adapter
      - name: Ruff
        run: |
          python -m pip install ruff
          ruff check integrations/browser-aios-adapter
      - name: Pytest
        run: python -m pytest integrations/browser-aios-adapter/tests -q
